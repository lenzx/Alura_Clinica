spring:
  profile.active: dev, test, prod
  datasource:
    url: jdbc:mysql://localhost/volmed-api
    username: root
    password: Localserver123...
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

server:
  error:
    include-stacktrace: never
api:
  security:
    secret: ${JWT_SECRET:123456}




#spring.datasource.url=jdbc:mysql://localhost/volmed-api
#spring.datasource.username=root
#spring.datasource.password=Localserver123...
#
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#
#server.error.include-stacktrace=never
#
#api.security.secret = ${JWT_SECRET:123456}
